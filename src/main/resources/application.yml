spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/accounting-db?useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org:
      springbootframeworks: debug
